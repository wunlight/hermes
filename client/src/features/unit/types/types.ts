export type UnitDTO = {
  id: string;
  code: string;
  name: string;
};

export type Unit = {
  id: string;
  code: string;
  name: string;
};

export type UnitOption = {
  id: string;
  name: string;
};

export type UnitReq = {
  code: string;
  name: string;
};

export type UnitFormModel = {
  code: string;
  name: string;
};
